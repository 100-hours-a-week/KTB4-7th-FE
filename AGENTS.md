# 맴매 프론트엔드 개발 규약

프론트엔드의 계획, 구현, 변경, 리뷰에는 `.agents/skills/memme-frontend-development/SKILL.md`를 적용한다.

## 공식 문서와 충돌 처리

- 기능 범위는 요구사항 분석, API 요청·응답·인증은 API 정의서, 화면 정책은 디자인·기획 문서를 최우선으로 한다.
- 기능 작업은 요구사항 분석 → API 정의서 → 디자인·기획 문서 → 기존 코드 순서로 최신 내용을 확인한다.
- 공식 문서와 코드가 충돌하거나 필요한 항목이 빠져 있으면 임의로 결정·수정하지 않는다. 충돌 항목과 영향을 알리고 승인받는다.
- 문서에 없는 API, 인증 흐름, V2/V3 기능을 추가하지 않는다.

## 기술과 프로젝트 구조

- React, TypeScript, Vite, Tailwind CSS를 사용한다.
- 서버 상태와 API 호출은 TanStack Query와 Axios를, 최소 전역 UI 상태는 Zustand를, 폼 검증은 React Hook Form과 Zod를 사용한다.
- `app`은 라우팅·Provider, `pages`는 페이지 조합, `features`는 사용자 기능, `entities`는 도메인 모델, `shared`는 재사용 UI·API·유틸리티를 담당한다.
- 여러 기능에서 재사용되지 않는 코드를 `shared`에 넣지 않는다. 기존 구조가 생기면 기존 구조를 우선한다.

## API·보안·품질 규칙

- URL, 메서드, 요청·응답 필드, 타입, 오류 처리, 인증 방식은 API 정의서와 일치시킨다.
- API 주소는 `VITE_API_BASE_URL`로 관리한다. 실제 비밀값과 `.env` 파일은 Git에 저장하지 않는다.
- 컴포넌트는 화면 표현에 집중하고, API 요청은 `shared/api` 또는 기능 내부 API 모듈에 분리한다.
- 의미 없는 축약어, 불필요한 주석, 요청 범위 밖 리팩터링을 피한다.
- 기능 변경에는 정상 흐름과 주요 오류·빈 상태를 검증하는 테스트를 작성한다. 테스트를 통과시키기 위해 기능 요구사항을 완화하지 않는다.
- 실행하지 못한 테스트·린트·빌드는 실행한 것처럼 보고하지 않는다.

## 브랜치·커밋·PR

- 모든 기능 개발은 GitHub Issue 생성 또는 기존 Issue 확인으로 시작한다. Issue에는 작업 범위와 완료 기준을 작성한다.
- 최신 `dev`에서 `feat/이슈번호-기능명` 브랜치를 만들고 그 브랜치에서만 구현·테스트·빌드를 진행한다. `dev`에서 직접 개발하지 않는다.
- PR은 `dev`를 대상으로 만들며 `main`을 대상으로 PR을 만들지 않는다.
- 구현 PR은 개발 시작 전에 만든 하나의 GitHub Issue와 연결하고, 본문에 `Closes #이슈번호`를 작성한다.
- 하나의 PR에는 하나의 작고 명확한 목적만 담는다. 관련 없는 파일이나 독립 기능을 섞지 않는다.
- 커밋은 의미 있는 작업 단위로 나누고 한글 Conventional Commit 형식으로 작성한다. 예: `feat: 회원가입 화면 구현`, `test: 회원가입 폼 검증 테스트 추가`.
- PR 제목은 한글로 작성한다. 본문에는 주요 변경사항, 구현 기능, 테스트 내용·결과, API 변경 여부와 `Closes #이슈번호`를 포함한다.
- PR 전 수정·신규 테스트, 관련 기존 테스트, 린트, 빌드가 모두 통과해야 한다.

## CI와 에이전트 작업 원칙

- `feat/*`에서 `dev`로 향하는 PR은 린트·테스트·빌드 CI를 통과해야 한다. 실패한 CI를 우회하거나 검증을 제거하지 않는다.
- `dev` 병합은 운영 배포가 아니다. 프론트 배포는 호스팅 방식과 승인 절차가 정해진 뒤 별도 워크플로로 구성한다.
- 작업 전 기존 코드와 최신 공식 문서를 확인하고, 기존 컨벤션을 우선한다.
- 기능 개발 요청을 받으면 먼저 연결할 GitHub Issue를 확인하거나 생성한다.
- 사용자가 PR 생성을 요청하면 Issue·브랜치·변경 범위·공식 문서 준수·테스트·린트·빌드·PR 대상 `dev`를 검증한다.
- 커밋·푸시·PR 생성 직전에 생성 예정 내용과 한글 커밋 메시지를 간단히 제시하고 최종 승인 1회를 받는다. 승인 후 commit → push → `dev` PR 생성을 연속 진행한다.
